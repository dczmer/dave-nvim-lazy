# Documentation tasks

When creating documentation for plugin configurations, create the document in `docs/` and follow the conventions used by other files in that directory.

Be sure to include:
- Important commands and key-bindings
- Examples
- Common use cases
